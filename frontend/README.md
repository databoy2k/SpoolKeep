# SpoolKeep — Frontend

React 19 + Vite frontend for SpoolKeep. Built assets are served by the Express backend in production; use `npm run dev` (from the project root) for the local dev server with HMR.

See the root `self-hosting.md` for deployment instructions.
